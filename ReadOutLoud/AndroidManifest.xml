<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comCS442.reader"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconrss_f"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="comCS442.reader.ArticleListActivity"
            android:label="@string/title_article_list" >
        </activity>
        <activity
            android:name="comCS442.reader.ArticleListFragment"
            android:label="@string/title_article_list" >
        </activity>
        
        <activity
            android:name="comCS442.reader.ArticleDetailActivity"
            android:label="@string/title_article_detail" >
        </activity>
        <activity
            android:name="comCS442.reader.ArticleDetailFragment"
            android:label="@string/title_article_detail" >
        </activity>

    </application>
    
     <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
 
    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    

</manifest>
